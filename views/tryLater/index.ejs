<!DOCTYPE html>
<html lang="en">

<head>
    <%- include("../partials/head.ejs") %>
</head>

<body class="yellow lighten-5">
    <div class="container">
        <header>
            <nav class="row lime">
                <%- include("../partials/nav.ejs") %>
            </nav>
            <div class="row center-align">
                <h1 class="col s12 grey-text text-darken-2">Saved Recipes</h1>
                <h5 class="col s12 grey-text text-darken-2">Feeling adventurous? Try something new!</h5>
            </div>
        </header>
        <div class="divider"></div><br><br>
        <main>
            <div class="section">
                <a class="btn lime hoverable" href="/recipes/new">Add a new recipe</a>
            </div>
            <div class="section row">
                <% for (i=0; i<tryLater.length; i++){%>
                <div class="col s6 m3 center-align">
                    <a class="white-text" href="/recipes/<%=tryLater[i]._id%>">
                    <div class="card z-depth-2 hoverable">
                        <div class="card-image">
                            <img src=" <% if (tryLater[i].img){%><%=tryLater[i].img%>
                                <%} else { %>
                                    <%="https://i.imgur.com/xzZK1Unb.jpg"%> 
                                <% } %>" alt="<%=tryLater[i].title%>">                        
                        </div>
                        <div class="card-action orange accent-2">
                            <h5><%=tryLater[i].title%></h5>
                        </div>
                    </div>
                    </a>
                </div>
                <% } %>
            </div>
        </main>
    </div>
</body>

</html>